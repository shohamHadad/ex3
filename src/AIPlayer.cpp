#include "AIPlayer.h"

AIPlayer::AIPlayer(char type): Player(type) {
}

AIPlayer::~AIPlayer() {
}

/**
 * function name: chooseSquare
 * input: vector<Square>
 * output: Square
 * operation: get a valid move from the user and returns it
 */
Square AIPlayer::chooseSquare(vector<Square> possibleMoves) {
	//
}
